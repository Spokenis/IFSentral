<parameter name="content" string="true"># TODO - Dockerizar IFSentral para deploy local/servidor

## Plano Aprovado

### Passos a serem executados:

- [X] **Passo 1:** Criar `wait-for-it.sh` — script para aguardar serviços ficarem prontos
- [ ] **Passo 2:** Atualizar `mosquitto.conf` — segurança com `allow_anonymous false` + password_file + acl_file
- [ ] **Passo 3:** Atualizar `Dockerfile` — otimizar camadas de build com cache, adicionar wait-for-it.sh
- [ ] **Passo 4:** Atualizar `docker-compose.yml` — restart policies, healthcheck, SMTP env vars, wait-for-it no worker, volumes Mosquitto
- [ ] **Passo 5:** Atualizar `src/config/.env` — valores genéricos para Docker, SMTP configurável via env
- [ ] **Passo 6:** Build e testes — `docker compose build` e `docker compose up -d`
</parameter>
